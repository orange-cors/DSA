#include <iostream>
#include <iomanip>
#include <cmath>
#include <string>
#include <math.h>
using namespace std;
int main(){
    int n;
    int i = 2;
    cin >> n;
    if ( n < 1 || n > 1000000);
    else
    while (n != -1) { // -1 là giá trị lính canh
    if ( n < 2);
    else{
    while ( i <= sqrt(n) ){
    if ( n % i == 0 ){
    cout << n << " Khong phai so nguyen to" << endl;
    break;
    }
    else{
    cout << n << " La so nguyen to" << endl;
    break;
        
    ++i;

    
    }
    ++i;
    }
}
}
return 0;
}